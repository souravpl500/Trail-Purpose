/* function UserProfile(props) {
  const { src, name, title, skills, logo, desc } = props;
  const style = {
    borderRadius: "50%",
    marginTop: "20px"
  };
  return (
    <div>
      <img src={src} alt="UserProfile-icon" width="10" style={style} />
      <h1>{name}</h1>
      <h2>{title}</h2>
      <h4>{skills}</h4>
      <img src={logo} />
      <p>{desc}</p>
    </div>
  );
}
export default UserProfile; */
